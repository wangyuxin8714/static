 日期对象创建
 var 变量名=new Date(你要设置的时间);  var time=new Date();console.log(time);
 toLocaleString() ==>按照当地的日期格式显示时间和日期(String)
 toLocaleDateString() 按照当地格式 显示 日期
 toString() 可以将除了null和undefined以外的所有类型转为字符串
 时间戳==> 1970年1月1日00:00:00 到现在的时间的毫秒形式
    日期对象.valueOf();  ==>将日期对象转时间戳
        console.log(time.valueOf());
        console.log(time*1);
        console.log(+time);
        console.log(time.getTime());
    日期对象.getFullYear() 获取年份
    日期对象.getMonth() 获取月份
    日期对象.getDate() 获取日期
    日期对象.getDay() 获取星期
    日期对象.getHours() 获取小时
    日期对象.getMinutes() 获取分钟
    日期对象.getSeconds() 获取秒数
    日期对象.getMilliSeconds() 获取毫秒
    setTime(设置时间(毫秒形式));
    setFullYear(设置的年份)
    setMonth(设置的月份)
    setDate(设置的日期);
    setHours(设置小时)
    setMinutes(设置分钟)
    setSeconds(设置秒)